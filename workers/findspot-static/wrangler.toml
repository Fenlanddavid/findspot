name               = "findspot-static"
main               = "index.ts"
compatibility_date = "2026-07-20"

[[r2_buckets]]
binding     = "STATIC_BUCKET"
bucket_name = "findspot-static"

[observability]
enabled = true

# Deploy:
#   cd workers/findspot-static
#   wrangler deploy
#
# Upload SM index metadata only after uploading and verifying range bundles:
#   wrangler r2 object put findspot-static/v2/sm-index/_meta.json --file scripts/out/sm-index/_meta.json --content-type application/json
# Upload AIM index metadata only after uploading and verifying bundles:
#   wrangler r2 object put findspot-static/v2/aim-index/_meta.json --file scripts/out/aim-index/_meta.json --content-type application/json
#
# Bulk-upload all SM range-bundle objects (bash):
#   find scripts/out/sm-index/bundles -type f | while read f; do
#     key="v2/sm-index/bundles/$(basename $f)"
#     wrangler r2 object put "findspot-static/$key" --file "$f"
#   done
