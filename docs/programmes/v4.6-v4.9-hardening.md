# FindSpot architecture hardening programme (v4.6-v4.9)

## Purpose

This document is the durable source of truth for the architecture hardening
programme. It is deliberately release-gated rather than calendar-based. A
workstream may take several patch releases, but the next workstream does not
start until the current workstream's exit criteria have been evidenced and
signed off.

Privacy, offline-first operation, deterministic behaviour and compatibility
with prior v4.x backups remain non-negotiable throughout.

## Release protocol

Each release slice follows the same sequence:

1. Add characterization tests before changing the implementation.
2. Make one cohesive change within the active workstream.
3. Add or tighten automated ratchets where the exit criterion is measurable.
4. Run targeted checks followed by the full checks required for the affected
   surfaces.
5. Update `package.json` and `src/version.ts` for the proposed release.
6. Present a sign-off report before pushing to `main`.
7. After explicit approval, push once and wait for the deploy and semantic tag.
8. Record the released version, commit, tag and evidence here before advancing.

Intermediate commits may live on a programme branch. They do not constitute a
release and must not be pushed to `main` as partial work.

## Sign-off model

- **Release sign-off:** required before every push to `main`. Covers one bounded
  slice, its tests, compatibility assessment, release notes and known risks.
- **Workstream exit sign-off:** required after all exit criteria for a
  workstream are met. The next workstream starts only after this approval.

The approval phrase `Approved - push <version>` authorizes that release only.

## Programme sequence

| Gate | Release lane | Workstream | Status |
| --- | --- | --- | --- |
| Entry | v4.5.1 | Programme entry verification and sync ADR | Complete |
| 1 | v4.6.x | A - Backup and data architecture | In progress |
| 2 | v4.7.x | C - Outstanding Questions state machine | Not started |
| 3 | v4.7.x | D - Worker protocol and reusable client | Not started |
| 4 | v4.8.x | F - Persistence boundaries | Not started |
| 5 | v4.8.x | I - Diagnostics and integrity checks | Not started |
| 6 | v4.8.x-v4.9.x | E - FieldGuide architecture | Not started |
| 7 | v4.9.x | J - Measurement-gated performance | Not started |

Workstream B is complete. Workstreams G (type safety) and H (testing) are
continuous ratchets delivered within the workstream that exposes the relevant
debt.

## Programme entry evidence

Verified against `v4.5.1` on 22 July 2026:

- `GeocodeCoordinator` has a Durable Object SQLite migration.
- The geocode and static workers use compatibility date `2026-07-20`.
- Vite declarations exist for `__APP_VERSION__` and `__BUILD_DATE__`.
- Deploy CI runs `npm test`, `npm run test:worker` and `npm run test:e2e`.
- Production geocoding returned `X-FindSpot-Cache: upstream` on the first
  request and `X-FindSpot-Cache: edge-cache` with `Cf-Cache-Status: HIT` on the
  identical second request.
- ADR 0001 accepts the local-only data model and defines reopen triggers.
- Unit baseline: 48 files passed; 711 tests passed; 1 skipped.

The BGS and Wales LiDAR workers retain older compatibility dates. Updating them
is separate worker maintenance and requires their own characterization and
deployment verification.

## Workstream A - Backup and data architecture

### Exit criteria

- [ ] No backup-related module exceeds approximately 500 lines.
- [ ] Every live Dexie table is centrally classified as `backup` or `excluded`.
- [ ] Every excluded table has a durable reason.
- [ ] A permanent test compares the registry with the live Dexie table list.
- [ ] Backup format versions and compatibility rules are explicit data.
- [ ] A real IndexedDB fixture passes export -> wipe -> restore in CI.
- [ ] The round-trip covers binary and damaged-media behaviour.
- [ ] Historical v4.x backup fixtures remain restorable.
- [ ] Restore remains atomic on validation, media and quota failure paths.
- [ ] Backup reminders escalate using finds changed since the last external
      backup.
- [ ] A restore drill validates and stages without replacing live data.
- [ ] Restore produces a per-table imported/skipped/repaired/damaged report.

### Planned slices

| Proposed release | Slice | Status | Sign-off |
| --- | --- | --- | --- |
| v4.6.0 | Characterization, backup format metadata and table registry | Released | Approved 2026-07-22 |
| v4.6.1 | Validation and normalization extraction | Released | Approved 2026-07-22 |
| v4.6.2 | Export and media archive extraction | Released | Approved 2026-07-22 |
| v4.6.3 | Import, staging and atomic restore extraction | Approved; deployment pending | Approved 2026-07-22 |
| v4.6.4 | Backup reminders, restore drill and recovery reporting | Not started | Pending |

Patch numbering is a proposal, not a reason to combine changes. If a slice
needs to be split to remain reviewable, later numbers move forward.

## Released: v4.6.0

Branch: `programme/v4.6.0-backup-registry`

Released from commit `8c511aa` and tagged `v4.6.0` after all GitHub Actions
jobs and the Pages deployment completed successfully on 22 July 2026.

Objective: make backup coverage and format ownership explicit without changing
export or restore behaviour.

### Acceptance checklist

- [x] Existing export shape and import defaults are characterized before the
      implementation is changed.
- [x] Current format version is defined once and used by export.
- [x] Supported historical formats are represented as explicit metadata.
- [x] A central registry classifies every current Dexie table.
- [x] Exclusions distinguish regenerable caches and local diagnostics, with a
      reason for each table.
- [x] Registry keys and live `FindSpotDB.tables` match exactly in a unit test.
- [x] Backed-up registry keys and validated backup table keys match exactly.
- [x] Adding an unclassified Dexie table fails the permanent suite.
- [x] Existing backup validation and round-trip tests remain green.
- [x] `npm test`, `npm run test:worker`, `npm run test:e2e:nix`, and
      `npm run build` pass before release sign-off.
- [x] No push, deploy or tag occurs before explicit release approval.

### Evidence log

| Date | Evidence | Result |
| --- | --- | --- |
| 2026-07-22 | Created programme branch from tagged `v4.5.1` | Pass |
| 2026-07-22 | Schema characterization records all 22 live Dexie tables | Pass |
| 2026-07-22 | Registry classifies 17 backup and 5 excluded tables | Pass |
| 2026-07-22 | `npm test`: 50 files, 724 passed, 1 skipped | Pass |
| 2026-07-22 | Worker tests: 9 passed | Pass |
| 2026-07-22 | Playwright/Nix: 41 passed | Pass |
| 2026-07-22 | Production build and release-note guard | Pass |
| 2026-07-22 | Release sign-off received for v4.6.0 | Approved |
| 2026-07-22 | GitHub Actions run 29917167091 and Pages deployment | Pass |
| 2026-07-22 | Annotated tag `v4.6.0` resolves to deployed commit `8c511aa` | Pass |

## Released: v4.6.1

Branch: `programme/v4.6.1-backup-validation`

Released from commit `230e1ba` and tagged `v4.6.1` after all GitHub Actions
jobs and the Pages deployment completed successfully on 22 July 2026.

Objective: extract backup schema, normalization and persistence validation from
`services/data.ts` behind its existing public API, without changing accepted
backup shapes, error behaviour or restore semantics.

### Acceptance checklist

- [x] Current normalization and validation behaviour is characterized before
      implementation moves.
- [x] Raw and validated backup types live in a dedicated backup schema module.
- [x] Missing optional tables normalize through the backed-up table registry.
- [x] Backup row and referential validation live under persistence validation.
- [x] `services/data.ts` retains compatibility exports for existing consumers.
- [x] Current version defaults, legacy JSON, zip manifests, retired question
      rules and unknown finite version behaviour remain unchanged.
- [x] Validation and schema modules remain below approximately 500 lines.
- [x] Existing backup, migration and browser restore tests remain green.
- [x] `npm test`, `npm run test:worker`, `npm run test:e2e:nix`, and
      `npm run build` pass before release sign-off.
- [x] No push, deploy or tag occurs before explicit release approval.

### Evidence log

| Date | Evidence | Result |
| --- | --- | --- |
| 2026-07-22 | `services/data.ts` reduced from 2,092 to 1,664 lines | Pass |
| 2026-07-22 | Largest extracted validation module: 346 lines | Pass |
| 2026-07-22 | Explicit `any` sites in touched area reduced from 32 to 23 | Pass |
| 2026-07-22 | Compatibility facade identity and version behavior tests | Pass |
| 2026-07-22 | `npm test`: 50 files, 731 passed, 1 skipped | Pass |
| 2026-07-22 | Worker tests: 9 passed | Pass |
| 2026-07-22 | Playwright/Nix: 41 passed | Pass |
| 2026-07-22 | Production build and release-note guard | Pass |
| 2026-07-22 | Release sign-off received for v4.6.1 | Approved |
| 2026-07-22 | GitHub Actions run 29918658910 and Pages deployment | Pass |
| 2026-07-22 | Annotated tag `v4.6.1` resolves to deployed commit `230e1ba` | Pass |

## Released: v4.6.2

Branch: `programme/v4.6.2-backup-export`

Released from commit `abc3ca5` and tagged `v4.6.2` after the approved release
reached `main` on 22 July 2026.

Objective: extract manifest generation and streamed media archive handling from
`services/data.ts` behind its existing public API, without changing data-only
JSON, full ZIP, progress, damaged-media or v4.x restore behaviour.

### Acceptance checklist

- [x] Data-only shape, empty-archive progress, media estimation and damaged
      media failure behaviour are characterized before implementation moves.
- [x] Manifest collection and export selection live in a dedicated export
      module.
- [x] MIME mapping, media estimation and streaming ZIP generation live in a
      dedicated media archive module.
- [x] The full archive remains manifest-first and reads media rows one at a
      time rather than retaining the whole library in memory.
- [x] `services/data.ts` retains compatibility exports for existing consumers.
- [x] The extracted export path contains no explicit `any` sites.
- [x] Every new backup module remains below approximately 500 lines.
- [x] Existing backup, migration and browser restore tests remain green.
- [x] `npm test`, `npm run test:worker`, `npm run test:e2e:nix`, and
      `npm run build` pass before release sign-off.
- [x] No push, deploy or tag occurs before explicit release approval.

### Evidence log

| Date | Evidence | Result |
| --- | --- | --- |
| 2026-07-22 | Characterization tests pass against the pre-extraction implementation | Pass |
| 2026-07-22 | `services/data.ts` reduced from 1,664 to 1,442 lines | Pass |
| 2026-07-22 | Export module: 84 lines; media archive module: 167 lines | Pass |
| 2026-07-22 | Compatibility facade identity tests for extracted exports | Pass |
| 2026-07-22 | `npm test`: 50 files, 736 passed, 1 skipped | Pass |
| 2026-07-22 | Worker tests: 9 passed | Pass |
| 2026-07-22 | Playwright/Nix: 41 passed | Pass |
| 2026-07-22 | Production build and release-note guard | Pass |
| 2026-07-22 | Release sign-off received for v4.6.2 | Approved |
| 2026-07-22 | Annotated tag `v4.6.2` resolves to released commit `abc3ca5` | Pass |

## Active release: v4.6.3

Branch: `programme/v4.6.3-backup-import`

Objective: extract backup input decoding, streamed staging and atomic live-data
replacement from `services/data.ts` behind its existing public API, without
changing import limits, progress, errors, v4.x compatibility or restore
semantics.

### Acceptance checklist

- [x] JSON import progress, validation-before-write, backed-up-versus-excluded
      tables and quota rollback are characterized before implementation moves.
- [x] A real IndexedDB fixture proves validation failure leaves live data
      unchanged and quota failure rolls the replacement transaction back.
- [x] Input detection, archive limits and in-memory zip decoding live in a
      dedicated import-input module.
- [x] Streamed zip parsing, temporary IndexedDB staging, media reconstruction
      and staging cleanup live in a dedicated restore-stage module.
- [x] Live replacement is one transaction whose table list is derived from the
      central backed-up table registry.
- [x] `services/data.ts` retains compatibility exports for existing consumers.
- [x] Existing binary-media, legacy JSON, large streamed-media and missing-media
      preservation coverage remains green.
- [x] The extracted import path contains no explicit `any` sites.
- [x] Every new backup module remains below approximately 500 lines.
- [x] `npm test`, `npm run test:worker`, `npm run test:e2e:nix`, and
      `npm run build` pass before release sign-off.
- [x] The PWA update message is concise and the release guard caps future
      messages at 80 characters.
- [x] No push, deploy or tag occurs before explicit release approval.

### Evidence log

| Date | Evidence | Result |
| --- | --- | --- |
| 2026-07-22 | Four characterization tests pass against the pre-extraction implementation | Pass |
| 2026-07-22 | `services/data.ts` reduced from 1,442 to 862 lines | Pass |
| 2026-07-22 | Largest new backup module: restore staging at 240 lines | Pass |
| 2026-07-22 | Extracted import path has no explicit `any` sites | Pass |
| 2026-07-22 | Compatibility facade and registry-derived transaction ratchets | Pass |
| 2026-07-22 | Targeted backup suites: 4 files, 95 tests passed | Pass |
| 2026-07-22 | `npm test`: 51 files, 742 passed, 1 skipped | Pass |
| 2026-07-22 | Worker tests: 9 passed | Pass |
| 2026-07-22 | Playwright/Nix: 41 passed | Pass |
| 2026-07-22 | Production build and concise release-note guard | Pass |
| 2026-07-22 | Release sign-off received for v4.6.3 | Approved |

## Later workstream exit criteria

### C - Outstanding Questions state machine

- One pure transition function and an explicit state/event table.
- Illegal transitions and terminal supersession invariants are tested.
- Runtime violations record non-fatal `diagLog` warnings.
- Direct lifecycle-status writes outside the transition module fail a permanent
  check.
- Existing question pipeline integration behaviour remains unchanged.

### D - Worker protocol and reusable client

- Request IDs, protocol versions, cancellation, timeout and typed error
  envelopes are shared.
- Terrain and landscape interpretation workers use the client.
- Worker construction is centralized and permanently checked.
- Round-trip, cancellation, timeout and version-mismatch tests pass.

### F - Persistence boundaries

- No engine imports `db`.
- UI reads may use live queries, but writes use intent-named service functions.
- Permanent checks enforce both boundaries without introducing a generic
  repository abstraction.

### I - Diagnostics and integrity checks

- Silent catches are eliminated, apart from the documented diagnostic sink
  exception.
- Shared integrity checks cover orphaned records, dangling permission IDs and
  retired rules.
- Checks run non-fatally after a schema-version change and surface one-line
  status in Settings.

### E - FieldGuide architecture

- `FieldGuide.tsx` is below approximately 500 lines.
- No FieldGuide hook exceeds approximately 300 lines.
- Scan orchestration is headless and consumes the worker client.
- UI modules contain no worker lifecycle or scan orchestration logic.

### J - Performance

- Every optimization begins with a retained realistic profile.
- Every shipped optimization records before/after measurements in its release
  notes.
- Every new cache uses shared TTL, engine-version invalidation and registry
  classification policy.

## Session handoff

Current task: push approved v4.6.3, verify the deployment and confirm the
semantic tag. On resumption, inspect this document, Git status, the current
branch and the latest semantic tag before editing.
