# FindSpot v4.12 review remediation

Date opened: 2026-07-27

Baseline: v4.12.6, including RRRA engineered-roads slice 2 and its
session-coverage clock-fixture correction.

Status: retrospective release ledger reconciled through v4.12.24

## Release ledger, v4.12.11–v4.12.24

This ledger was reconstructed from the committed `package.json` version bumps,
`src/version.ts` update copy and adjacent implementation commits. It records
what the repository proves; it does not retroactively claim release-specific
test matrices that were not written down at the time.

| Version | Date | Evidence commit(s) | Recorded change |
| --- | --- | --- | --- |
| 4.12.11 | 2026-08-01 | `576886c`, `0a90026` | Architecture-dump integrity follow-up and testing update. |
| 4.12.12 | 2026-08-03 | `a7d2c58`, `517ac3`, `a1d404c` | Persist the preferred FieldGuide startup basemap. |
| 4.12.13 | 2026-08-03 | `f518bb9` | Link Android beta testers to the Google Play internal test. |
| 4.12.14 | 2026-08-03 | `cc8d46a` | Update Companion Play smoke coverage and tester access copy. |
| 4.12.15 | 2026-08-03 | `bec103d` | Control background Companion tracking from FindSpot. |
| 4.12.16 | 2026-08-03 | `930c894` | Import completed Companion trails into their sessions. |
| 4.12.17 | 2026-08-03 | `4bb6812` | Centre permission thumbnails on mapped boundaries. |
| 4.12.18 | 2026-08-03 | `1e373ef` | Open active sessions at the top of the session page. |
| 4.12.19 | 2026-08-03 | `49d658a` | Clarify how Companion beta access is obtained. |
| 4.12.20 | 2026-08-04 | `6ab7cad`, `586a39d` | Add the standalone Roman-road layer, opacity control and road details. |
| 4.12.21 | 2026-08-04 | `1172e57`, `14173db` | Fix field-report input clearing, improve startup work and stabilize its test. |
| 4.12.22 | 2026-08-10 | this release | Add local Surface Scatter capture and permission-scoped Landscape investigations display, with no FieldGuide or scoring influence. |
| 4.12.23 | 2026-08-11 | `accebd4` | Expand Surface Scatter capture and its permission-level review workflow. |
| 4.12.24 | 2026-08-11 | `eb651dc` | Add GPS and map-selected locations to Surface Find records. |

The commits above are repository evidence, not semantic release tags; the tag
history remains separate and must not be inferred from this table.

## v4.12.22 — Surface observations

- Adds two-tap, GPS-backed surface-find capture during an active session, with
  optional period enrichment on the same centred dialog.
- Displays active records only in the matching Permission page's `Landscape
  investigations` card under explicit `Observed by you` provenance.
- Keeps Surface Scatter completely outside FieldGuide components, engine
  evidence, scores, confidence, targets, rankings and ordering. A boundary test
  protects this separation.
- Uses the PAS-aligned broad-period vocabulary and normalizes the superseded
  `anglo_saxon` and `prehistoric` prototype values without recording a false
  reassessment.
- Adds local `surfaceObservations` persistence, backup-format v8 support,
  restore validation, lifecycle cleanup and integrity auditing.
- Accepts both epoch-number and ISO-string timestamps from prototype local
  records, preventing multiple saved observations from crashing a Permission
  page.
- Excludes retired observations everywhere and keeps the pottery-plus-CBM rule
  descriptive, period-aware and constrained by known permission sections.
- Disables service-worker registration during Vite development and unregisters
  stale development workers, restoring reliable React Fast Refresh without
  changing production PWA behaviour.
- Architecture dumps now include a checksummed manifest and required public
  assets, making omissions and curated extracts directly detectable.

Release validation for v4.12.22:

- `npm test`: 109 files passed; 996 tests passed; 1 intentional skip.
- Type floor: 890, within the 891 ceiling.
- Explicit `any`: 137 nodes, unchanged from the ratchet.
- Architecture dump: all 518 committed selected files covered.
- Production PWA build completed successfully.
- FieldGuide integration files have no diff from v4.12.21.

## Compatibility guardrails

- No Dexie schema version change.
- No backup table or backup-format change.
- Existing user-authored coverage, prediction, and calibration records remain
  intact.
- Regenerable geology cache records may be invalidated by an explicit
  classifier-version bump.
- Previously resolved prediction outcomes are not reclassified.

## DS decisions

### Find/prediction time gate

Decision: strict event-time gate.

A valid `foundAt` is authoritative. When it is absent or invalid, a valid
`createdAt` is the fallback. A find whose authoritative timestamp predates the
prediction's `surfacedAt` does not count as a hit. This prevents recording
backlogs from inflating calibration while retaining a safe fallback for legacy
finds without a usable event time.

The change applies to future resolution attempts, including predictions that
are still unresolved when the change lands. Existing resolved outcomes and
aggregates are not rewritten.

### Session-coverage edit deadline

Decision: re-derive editability when the 48-hour deadline passes while a view
is open.

Both permission-level and direct session review entry points switch to
read-only state live. Mutation-side deadline validation remains the enforcement
boundary.

### Geology modifier representation

Decision: honest scalar simplification.

The seven-slot table is replaced with one bounded modifier per geology class.
Current effective scores are retained, so user-visible ranking behaviour does
not change. The regenerable geology cache classifier version is bumped because
the cached context shape changes.

### Coverage calibration thresholds

Decision: retain the current conservative thresholds pending field evidence.

No section-size, reported-confirmation, or tracked-coverage threshold changes
are included in this remediation. Any later change requires a separate slice
that distinguishes section layout from negative-evidence confidence and
explicitly preserves existing coverage history.

## Slice sign-off

### R1 — diagnostics integrity and field performance

- BGS layer fetches now distinguish timeout, request failure, invalid response,
  genuine empty response, and parsed data. A failed layer cannot be combined
  with a successful layer into a partial classification.
- Request and response failures reach the Field Guide warning path and are
  audited as `request_fail` or `invalid_response`. Two valid empty feature
  collections retain the existing `empty_response` meaning.
- Audit-history caveat: before this release, some network and CORS failures
  were recorded as `empty_response`. Queries spanning the release boundary
  must not interpret all historic `empty_response` rows as confirmed data-free
  tiles.
- Reconciliation of unchanged coverage sections performs zero calls to
  `deriveSectionCandidates`. The fixture fast path has a 50 ms ceiling to avoid
  a brittle wall-clock ratchet; zero derivations is the primary performance
  invariant.

### R2 — behaviour consistency

- Genuine empty geology tiles use a validated marker in the existing geology
  cache store and observe the populated-entry TTL. The second fixture scan
  performs no BGS fetch.
- Permission and session coverage views use the same accessible preparation
  error component.
- Both views re-evaluate editability at the next 48-hour deadline while open;
  the mutation command remains authoritative if a race occurs.
- Prediction matching uses valid `foundAt` as authoritative and falls back to
  `createdAt` only for legacy records without a valid find time. No historic
  resolution or calibration row is rewritten.

### R3 — proxy and infrastructure hygiene

- The BGS Worker reads its origin allowlist from `ALLOWED_ORIGINS`, rejects
  absent and unlisted origins, echoes only a matched origin, and adds
  `Vary: Origin`.
- Edge-cache entries remain origin-neutral; CORS headers are applied to the
  response after the cache lookup, preventing one allowed origin from leaking
  into another origin's response.
- The automated matrix covers production browser/PWA origin, localhost,
  preflight, cache hits, missing Origin, and an unlisted origin.
- Staged-preview and post-deploy Chromium requests from the real production
  origin and localhost both read a 200 BGS GML response. Installed PWA traffic
  uses that same production origin and cross-origin request path.
- Live cache-hit probes echoed the production and localhost origins
  independently. Missing and unlisted origins returned 403, and an allowed
  preflight returned 204 with `Vary: Origin`.
- BGS attribution is updated to 2026 and app display copy is centralised in one
  shared constant.
- All CI jobs use Node 22.22.2.
- Wrangler 4.114.0 dry-run bundle: 11.12 KiB upload, 3.01 KiB gzip.

### R4 — geology modifier representation

- The decorative seven-slot representation is replaced by one scalar per
  geology class. Characterization tests pin all class outputs.
- Scalars preserve the previous effective net values, so hotspot scores,
  gating, ordering, and confidence thresholds do not change for current users.
- The geology classifier/cache version is 3 because the cached context shape
  changed. This invalidates only regenerable geology cache entries.
- The obsolete “Phase 1: all zero” comment is removed.

## Final validation matrix

- `npm test`: 99 files passed; 933 tests passed; 1 intentional skip.
- Type floor: 883, within the 891 ceiling.
- Explicit `any`: 137 nodes, unchanged from the ratchet.
- Architecture dump: all 438 files covered.
- `npm run test:worker:nix`: Worker types current; 10 tests passed.
- `npm run test:e2e:nix`: 52 Chromium tests passed.
- `npm run build`: production PWA build completed; 53 precache entries.
- `npm run check:release`: proposed v4.12.7 metadata passed.
- `npx wrangler deploy --dry-run --config workers/bgs-proxy/wrangler.toml`:
  bundle completed without deployment.
- `git diff --check`: passed.

## Deployment safety

The BGS Worker was uploaded first as an isolated preview and then promoted to
100% after the preview matrix passed:

- Active version: `34e8223a-f99c-4ec8-af94-3ec4e467d17a`.
- Previous rollback version: `0444fb4f-f7c3-4e13-8628-387ef5261056`.
- Deployment time: 2026-07-27 14:46 UTC.
- Observability is enabled at full sampling.

The existing v4.12.6 app continued to read BGS responses after promotion, so
Worker-first rollout did not interrupt current users. The app has not been
pushed. No database migration, backup migration, or user-authored data rewrite
is required.

### v4.12.7 release preparation

- Proposed version: 4.12.7.
- Update note: “Coverage opens faster, and geology outages are now reported
  accurately.”
- The complete `npm test` gate and production build were repeated after the
  version bump with the same green results.
- GitHub connectivity is available, but no commit or push has been made.
- The BGS Worker preview and live matrices passed; the verified Worker is now
  serving production traffic.
- The application release remains gated on the explicit
  `Approved - push v4.12.7` sign-off phrase.
