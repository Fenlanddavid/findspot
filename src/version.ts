// ─── Release notes ────────────────────────────────────────────────────────────
// Update this before each push. Shown in the PWA update banner so users know
// what the update contains before they tap "Update Now".

export const UPDATE_NOTES = 'backup format ownership and a permanent guard ensuring every data table has an explicit backup decision';
