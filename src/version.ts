// ─── Release notes ────────────────────────────────────────────────────────────
// Update this before each push. Shown in the PWA update banner so users know
// what the update contains before they tap "Update Now".

export const UPDATE_NOTES = 'FieldGuide redesigned — landscape analysis now shows a reading summary, signal breakdown, and suggested field approach';
